const pattern=document.querySelector('.pattern'),orb=document.querySelector('.cursor-orb');let y=0,target=0;addEventListener('scroll',()=>target=scrollY*.11,{passive:true});(function tick(){y+=(target-y)*.06;if(pattern)pattern.style.backgroundPosition='center '+y+'px';requestAnimationFrame(tick)})();addEventListener('mousemove',e=>{orb.style.left=e.clientX+'px';orb.style.top=e.clientY+'px'});