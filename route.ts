import {NextResponse} from "next/server";export async function POST(req:Request){try{const body=await req.json();const {name,email,business,message}=body;if(!name||!email||!business||!message)return NextResponse.json({error:"Missing required fields"},{status:400});if(typeof email!=="string"||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return NextResponse.json({error:"Invalid email"},{status:400});/* Production hook: persist lead + send transactional notification here. Do not log sensitive lead data. */return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Invalid request"},{status:400});}}